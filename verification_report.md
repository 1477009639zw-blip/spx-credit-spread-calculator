# SPX 信用价差策略计算器验证报告

## 验证结论

- 已用网页同一套 `core.js` 逻辑做独立验证
- 验证方式：`core.js` + `verify_core.js` 通过本机 JavaScript 解释器执行
- 验证样本：`10,010` 组
  - 边界样本：`10` 组
  - 随机样本：`10,000` 组
- 结果：`全部通过`

## 抽样结果

- 示例输入：
  - `SPX 昨收 = 6000`
  - `VIX1D 昨收 = 25`
  - `今日开盘 = 5991`
- 计算输出：
  - `方向 = PUT`
  - `final OTM = 2.2677868381%`
  - `精确公式点位 = 5863.9328`
  - `向外保守 5 点 = 5860`
  - `向内 5 点 = 5865`

## 备注

- 页面主结果使用“精确公式点位”
- 同时展示“向外保守 5 点”和“向内 5 点”，方便你快速对比不同执行口径
- 页面同时展示基于昨收的固定价外参考点位：
  - `±1.5%`
  - `±2.0%`
  - `±2.5%`
  - `±3.0%`
- 页面支持本地浏览器记忆保存：
  - 可按日期保存当前输入
  - 可从记录卡片一键载入并重算
  - 同一日期再次保存会覆盖旧值
- 每条记录支持备注
- 已验证导出功能：
  - `CSV` 导出包含备注与策略字段
  - `Excel` 导出包含备注与策略字段
- 已加入 Supabase 云端同步：
  - Supabase URL 已配置为 `https://frnrhycstwiezcifktnh.supabase.co`
  - 使用 publishable key 初始化前端客户端
  - 登录方式为邮箱 Magic Link
  - 登录后可上传本地记录、从云端载入记录、保存时自动同步、删除时同步删除
  - 表结构与 RLS 策略见 `supabase_schema.sql`
- 低 VIX override 规则：
  - `t-1 VIX1D < 15` 且 `gap down` 未超过 `-0.5%` 时，固定做 `CALL`
  - 若 `gap down <= -0.5%`，则豁免 override，回到正常 `PUT`
